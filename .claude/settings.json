{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/muhammadrifqi/Documents/Ruang Pacaran\" && git add -A && (git diff --cached --quiet || (git commit -m \"auto: $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin HEAD)) 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Menyimpan ke GitHub..."
          }
        ]
      }
    ]
  }
}
